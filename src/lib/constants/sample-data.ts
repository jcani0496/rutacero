/**
 * Marker prefix stored in the `notes` field of every row created by the
 * sample-data preview ("Ver con datos de ejemplo"). It lets us identify and
 * bulk-delete sample rows later without a schema change.
 */
export const SAMPLE_DATA_PREFIX = '[EJEMPLO]';
