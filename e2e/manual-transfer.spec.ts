import { test } from '@playwright/test';

// F6: suite retired pending rewrite against better-auth + DATABASE_URL.
test.describe.skip('manual-transfer (pending F6 rewrite)', () => {
  test('placeholder', async () => {});
});
