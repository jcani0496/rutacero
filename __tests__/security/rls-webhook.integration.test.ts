import { describe, it } from 'vitest';

/** F6: skipped — required Supabase local RLS stack. */
describe.skip('rls-webhook integration (retired with Supabase local)', () => {
  it('placeholder', () => {});
});
