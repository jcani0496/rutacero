import { describe, it } from 'vitest';

/** F6: skipped — required Supabase local RLS stack. */
describe.skip('billing-entitlements RLS (retired with Supabase local)', () => {
  it('placeholder', () => {});
});
